---
phase: 04-content-i18n-and-sharing
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [index.html, scripts/verify-phase-4-i18n-share.js]
autonomous: true
requirements: [SHR-01, SHR-02, SHR-03, SHR-04, SHR-05]

must_haves:
  truths:
    - "Every calculated result can be serialized into a stable hash URL and restored directly into the results view"
    - "Shared result links open the results state first, without flashing the input form before hydration"
    - "X, WhatsApp, and LinkedIn share buttons are driven by pure URL builders using the current result and language"
    - "Static OG/Twitter metadata remains present while result-specific share text comes from localized builder functions"
  artifacts:
    - path: "index.html"
      provides: "Hash serialization/hydration flow, share UI, and platform-specific share builders"
      contains: "function serializeShareState(state)"
      min_lines: 1080
    - path: "scripts/verify-phase-4-i18n-share.js"
      provides: "Phase 4 verifier extended for share URL and hash restoration checks"
      contains: "serializeShareState"
      min_lines: 180
  key_links:
    - from: "showResults(result)"
      to: "syncHashWithResult(result)"
      via: "showing a result updates the shareable URL state"
      pattern: "syncHashWithResult\\(result\\)"
    - from: "init()"
      to: "hydrateFromHash()"
      via: "boot path decides the first visible view before removing boot gating"
      pattern: "hydrateFromHash\\("
    - from: "share button click handlers"
      to: "buildXShareUrl/buildWhatsAppShareUrl/buildLinkedInShareUrl"
      via: "result-specific share links are created from current stable ids and language"
      pattern: "share-x-btn|share-whatsapp-btn|share-linkedin-btn"
---

<objective>
Finish Phase 4 with direct-result shared links and working social share buttons for X, WhatsApp, and LinkedIn.

Purpose: The product’s distribution loop depends on people sharing exactly the result they saw, in the language they saw it, without a broken first-load experience.
Output: `index.html` can serialize result state into the hash, hydrate from shared URLs before first visible paint, and open platform share flows with localized result text and the encoded result URL.
</objective>

<execution_context>
@/home/giuice/.codex/get-shit-done/workflows/execute-plan.md
@/home/giuice/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-content-i18n-and-sharing/04-RESEARCH.md
@.planning/phases/04-content-i18n-and-sharing/04-VALIDATION.md
@.planning/phases/04-content-i18n-and-sharing/04-02-PLAN.md
@index.html
@scripts/verify-phase-4-i18n-share.js

<interfaces>
This plan assumes:

- `currentLanguage` exists and is persisted
- localized rendering already works
- `lastResult` stores the currently shown pure result object

The hash contract must use only stable ids:

```text
#job=<occupation-id>&exp=<experience-id>&lang=<en|ptbr>
```

The share builders must target these exact base URLs:

- X: `https://twitter.com/intent/tweet`
- WhatsApp: `https://api.whatsapp.com/send`
- LinkedIn: `https://www.linkedin.com/sharing/share-offsite/?url=`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add share actions and pure share/hash helpers</name>
  <files>index.html</files>
  <read_first>
    - index.html
    - .planning/phases/04-content-i18n-and-sharing/04-RESEARCH.md
    - .planning/phases/04-content-i18n-and-sharing/04-02-PLAN.md
  </read_first>
  <action>
Edit `index.html` to add a share-actions block inside the results view and implement pure state/URL builders in `// ===== SHARE =====`.

Add this exact block above the existing `.try-again-container`:

```html
<section class="share-actions" id="share-actions">
  <p class="results-card-heading" id="share-heading">Share This Doom</p>
  <div class="share-actions-row">
    <button type="button" class="share-btn" id="share-x-btn">X / Twitter</button>
    <button type="button" class="share-btn" id="share-whatsapp-btn">WhatsApp</button>
    <button type="button" class="share-btn" id="share-linkedin-btn">LinkedIn</button>
  </div>
</section>
```

Add CSS for:
- `.share-actions`
- `.share-actions-row`
- `.share-btn`
- `.share-btn:hover`

Style them using existing tokens:
- border/text default: `var(--neon-cyan)`
- hover glow: `var(--glow-cyan)`
- responsive layout: wrap to multiple rows below 480px

In `// ===== SHARE =====`, add these exact helpers:

```js
function serializeShareState(state) { ... }
function parseShareState(hash) { ... }
function buildShareState(result) { ... }
function buildBasePageUrl() { ... }
function buildStateUrl(state) { ... }
function buildShareMessage(result, language) { ... }
function buildXShareUrl(result, language) { ... }
function buildWhatsAppShareUrl(result, language) { ... }
function buildLinkedInShareUrl(result) { ... }
function openShareWindow(url) { ... }
```

Implementation rules:
- `serializeShareState(state)` must encode only `job`, `exp`, and `lang`
- `buildShareState(result)` must use:
  - `result.occupation.id`
  - `result.seniority.experienceId`
  - `currentLanguage`
- `buildBasePageUrl()` must return `window.location.origin + window.location.pathname`
- `buildStateUrl(state)` must return `buildBasePageUrl() + '#' + serializeShareState(state)`
- `buildShareMessage(result, language)` must return a localized one-line summary containing:
  - localized occupation label
  - localized seniority label
  - countdown years/months/days
  - localized result URL on a new sentence
- `buildXShareUrl(result, language)` must append `text=` and `url=` query params to `https://twitter.com/intent/tweet`
- `buildWhatsAppShareUrl(result, language)` must append `text=` to `https://api.whatsapp.com/send`
- `buildLinkedInShareUrl(result)` must return `https://www.linkedin.com/sharing/share-offsite/?url=` + encoded result URL
- `openShareWindow(url)` must call `window.open(url, '_blank', 'noopener,noreferrer')`

Bind click handlers in `init()` for:
- `share-x-btn`
- `share-whatsapp-btn`
- `share-linkedin-btn`

Each handler must no-op if `lastResult` is missing.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const html=fs.readFileSync('index.html','utf8');['id=\"share-x-btn\"','id=\"share-whatsapp-btn\"','id=\"share-linkedin-btn\"','function serializeShareState(state)','function parseShareState(hash)','function buildShareState(result)','function buildStateUrl(state)','function buildShareMessage(result, language)','function buildXShareUrl(result, language)','https://twitter.com/intent/tweet','https://api.whatsapp.com/send','https://www.linkedin.com/sharing/share-offsite/?url=','window.open(url, \\\"_blank\\\", \\\"noopener,noreferrer\\\")'].forEach((snippet)=>{if(!html.includes(snippet)) throw new Error('missing '+snippet);}); console.log('phase 4 share helpers present');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="share-x-btn"' index.html` returns 1
    - `grep -c 'id="share-whatsapp-btn"' index.html` returns 1
    - `grep -c 'id="share-linkedin-btn"' index.html` returns 1
    - `grep -c 'function serializeShareState(state)' index.html` returns 1
    - `grep -c 'function buildShareMessage(result, language)' index.html` returns 1
    - `grep -c 'https://twitter.com/intent/tweet' index.html` returns at least 1
    - `grep -c 'https://api.whatsapp.com/send' index.html` returns at least 1
    - `grep -c 'https://www.linkedin.com/sharing/share-offsite/?url=' index.html` returns at least 1
    - `grep -c "window.open(url, '_blank', 'noopener,noreferrer')" index.html` returns at least 1
  </acceptance_criteria>
  <done>The results view now exposes concrete share controls and pure helpers for share text, share URLs, and stable hash state</done>
</task>

<task type="auto">
  <name>Task 2: Hydrate from hash before first visible paint and extend the verifier for share-state coverage</name>
  <files>index.html, scripts/verify-phase-4-i18n-share.js</files>
  <read_first>
    - index.html
    - scripts/verify-phase-4-i18n-share.js
    - .planning/phases/04-content-i18n-and-sharing/04-VALIDATION.md
  </read_first>
  <action>
Continue editing `index.html` and extend `scripts/verify-phase-4-i18n-share.js`.

In `index.html`, implement boot gating and hash hydration with these exact changes:

1. Add `data-booting="true"` to the opening `<body>` tag.
2. Add CSS:

```css
body[data-booting='true'] #input-view,
body[data-booting='true'] #results-view {
  display: none !important;
}
```

3. Add these exact helpers:

```js
function syncHashWithResult(result) { ... }
function clearResultHash() { ... }
function hydrateFromHash() { ... }
```

Rules:
- `syncHashWithResult(result)` must call `history.replaceState(null, '', buildStateUrl(buildShareState(result)))`
- `clearResultHash()` must call `history.replaceState(null, '', buildBasePageUrl())`
- `hydrateFromHash()` must:
  - parse `window.location.hash`
  - resolve initial language via `resolveInitialLanguage(parsedHash)`
  - validate `job` against `getOccupationById(parsedHash.job)`
  - validate `exp` against `getSeniorityByExperienceId(parsedHash.exp)`
  - if both are valid:
    - build a result with `buildResultModel(parsedHash.job, parsedHash.exp)`
    - call `applyLanguage(...)`
    - call `showResults(result)`
    - return `true`
  - otherwise return `false`

4. Update `showResults(result)` so after rendering it calls `syncHashWithResult(result)`.
5. Update the Try Again flow so it calls `clearResultHash()` before returning to input view.
6. Update `init()` so it:
   - renders the app once
   - calls `hydrateFromHash()`
   - if hydration returns `false`, shows the input view
   - removes `data-booting` from `<body>` only after the first visible view has been chosen

Extend `scripts/verify-phase-4-i18n-share.js` so it also asserts:
- `body[data-booting='true']` CSS exists
- `function hydrateFromHash()` exists
- `function syncHashWithResult(result)` exists
- `function clearResultHash()` exists
- hash keys `job`, `exp`, and `lang` appear in the serializer/parser
- `history.replaceState(null, '', buildBasePageUrl())` exists
- share URLs contain the encoded result URL
- `buildLinkedInShareUrl(` uses only the result URL, not a custom `text=` param

Run:

```bash
node scripts/verify-phase-2-results.js
node scripts/verify-phase-4-i18n-share.js
```
  </action>
  <verify>
    <automated>node scripts/verify-phase-2-results.js && node scripts/verify-phase-4-i18n-share.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "body\\[data-booting='true'\\]" index.html` returns at least 1
    - `grep -c 'function hydrateFromHash()' index.html` returns 1
    - `grep -c 'function syncHashWithResult(result)' index.html` returns 1
    - `grep -c 'function clearResultHash()' index.html` returns 1
    - `grep -c "params.set('job'" index.html` returns at least 1
    - `grep -c "params.set('exp'" index.html` returns at least 1
    - `grep -c "params.set('lang'" index.html` returns at least 1
    - `grep -c "history.replaceState(null, '', buildBasePageUrl())" index.html` returns at least 1
    - `node scripts/verify-phase-2-results.js` exits 0
    - `node scripts/verify-phase-4-i18n-share.js` exits 0
  </acceptance_criteria>
  <done>Shared result URLs restore the correct localized results view before first visible paint, and share buttons now point at deterministic platform URLs</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/verify-phase-2-results.js` exits 0
- [ ] `node scripts/verify-phase-4-i18n-share.js` exits 0
- [ ] Shared hash URLs restore the correct result without showing the input form first
- [ ] X, WhatsApp, and LinkedIn buttons open their expected share flows with the encoded result URL
</verification>

<success_criteria>
- X, WhatsApp, and LinkedIn share buttons are live on the results screen
- Shared URLs encode result state in the hash and restore it correctly
- Boot flow avoids the input-view flash on valid shared links
- OG/Twitter metadata remains present for generic rich previews
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-i18n-and-sharing/04-03-SUMMARY.md`
</output>
