---
phase: 04-content-i18n-and-sharing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, scripts/verify-phase-4-i18n-share.js, .planning/phases/04-content-i18n-and-sharing/04-roast-matrix.json, og-preview.png]
autonomous: true
requirements: [I18N-02, I18N-03, SHR-05]

must_haves:
  truths:
    - "All user-facing copy and roast components exist in both English and PT-BR under stable locale keys"
    - "The Phase 2 engine stays locale-agnostic and deterministic; localization happens in I18N/UI helpers instead of ENGINE"
    - "Static Open Graph and Twitter meta tags exist in the shipped HTML head for launch-ready link previews"
    - "A fast Node-only verifier can prove bilingual catalog completeness, bilingual roast coverage, and metadata presence"
  artifacts:
    - path: "index.html"
      provides: "Bilingual locale catalogs, localized roast helpers, and static OG/Twitter metadata"
      contains: "const LANGUAGE_STORAGE_KEY = 'jobDeathClockLanguage'"
      min_lines: 900
    - path: "scripts/verify-phase-4-i18n-share.js"
      provides: "Phase 4 Node-only i18n/share verifier"
      contains: "Phase 4 i18n/share verified:"
      min_lines: 120
    - path: ".planning/phases/04-content-i18n-and-sharing/04-roast-matrix.json"
      provides: "Deterministic bilingual roast matrix artifact"
      contains: "\"matrixSize\": 84"
      min_lines: 50
  key_links:
    - from: "index.html (I18N catalogs)"
      to: "scripts/verify-phase-4-i18n-share.js"
      via: "verifier extracts locale data and bilingual roast helpers from the shipped HTML"
      pattern: "ROAST_LIBRARY|buildLocalizedRoast|LANGUAGE_STORAGE_KEY"
    - from: "buildResultModel"
      to: "buildLocalizedRoast"
      via: "localized roast builder consumes stable ids from the pure result object without mutating ENGINE"
      pattern: "result\\.occupation\\.id|result\\.seniority\\.id|result\\.risk\\.key"
    - from: "meta[property='og:title'] and meta[name='twitter:card']"
      to: "share platforms"
      via: "static preview metadata shipped in the HTML response"
      pattern: "og:title|og:description|twitter:card|twitter:title"
---

<objective>
Create the Phase 4 foundation inside `index.html` and a matching Node verifier: bilingual copy catalogs, bilingual roast composition helpers, and static social preview metadata.

Purpose: Phase 4 execution will touch localization, result sharing, and hash boot logic. The safest start is to lock the bilingual content model and verification surface first, while keeping the Phase 2 engine contract stable.
Output: `index.html` contains launch-ready locale catalogs and OG/Twitter tags; `scripts/verify-phase-4-i18n-share.js` proves catalog completeness and bilingual roast coverage; `.planning/phases/04-content-i18n-and-sharing/04-roast-matrix.json` exists for content QA; `og-preview.png` exists for share cards.
</objective>

<execution_context>
@/home/giuice/.codex/get-shit-done/workflows/execute-plan.md
@/home/giuice/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-content-i18n-and-sharing/04-RESEARCH.md
@.planning/phases/04-content-i18n-and-sharing/04-VALIDATION.md
@index.html
@scripts/verify-phase-2-results.js

<interfaces>
Current constraints from `index.html`:

- `// ===== ENGINE =====` already returns `occupation.id`, `seniority.id`, `risk.key`, and English `roast`
- `// ===== I18N =====` currently contains only `const STRINGS = { en: {}, ptbr: {} };`
- `<head>` currently has only the document title and Google Fonts links; no OG/Twitter metadata exists
- The public site URL is `https://giuice.github.io/risk-jobs/`

The new content layer must not move DOM access into `ENGINE`, and `scripts/verify-phase-2-results.js` must still execute successfully after this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the empty I18N placeholder with bilingual catalogs and localized roast helpers</name>
  <files>index.html</files>
  <read_first>
    - index.html
    - .planning/phases/04-content-i18n-and-sharing/04-RESEARCH.md
    - scripts/verify-phase-2-results.js
  </read_first>
  <action>
Edit `index.html` and replace the current `// ===== I18N =====` placeholder with a full bilingual content layer while leaving `// ===== ENGINE =====` untouched except for reading its existing result shape.

Add these exact constants and helpers immediately under `// ===== I18N =====`:

1. `const DEFAULT_LANGUAGE = 'en';`
2. `const LANGUAGE_STORAGE_KEY = 'jobDeathClockLanguage';`
3. `const SUPPORTED_LANGUAGES = ['en', 'ptbr'];`
4. `const STRINGS = { en: {...}, ptbr: {...} };` with concrete keys for:
   - `documentTitle`
   - `inputTitle`
   - `inputSubtitle`
   - `professionLabel`
   - `experienceLabel`
   - `ctaLabel`
   - `resultsTitle`
   - `shelfLifeHeading`
   - `automationDangerHeading`
   - `finalVerdictHeading`
   - `countdownYears`
   - `countdownMonths`
   - `countdownDays`
   - `safeLabel`
   - `doomedLabel`
   - `tryAgainLabel`
   - `riskPrefix`
   - `shareHeading`
   - `shareXLabel`
   - `shareWhatsAppLabel`
   - `shareLinkedInLabel`
   - `languageLabel`
   - `languageEnglish`
   - `languagePortuguese`
5. `const OCCUPATION_LABELS = { en: {...}, ptbr: {...} };` with all 21 occupation ids exactly matching `OCCUPATIONS`.
6. `const SENIORITY_LABELS = { en: {...}, ptbr: {...} };` with keys `junior`, `mid`, `senior`, `architect`.
7. `const SENIORITY_RANGE_LABELS = { en: {...}, ptbr: {...} };` with keys `years-0-2`, `years-3-5`, `years-6-9`, `years-10-plus`.
8. `const RISK_LABELS = { en: {...}, ptbr: {...} };` with keys `low`, `watch`, `exposed`, `critical`, `doomed`.
9. `const ROAST_LIBRARY = { en: { openers, middles, closers }, ptbr: { openers, middles, closers } };`
   - Preserve the existing 21 occupation ids, 5 risk keys, and 4 seniority ids exactly.
   - English values may reuse the current tone.
   - PT-BR values must be translated/adapted, not literal English clones.

Add these exact helper functions after the catalogs:

```js
function isSupportedLanguage(language) { ... }
function t(key, language) { ... }
function getOccupationLabel(occupationId, language) { ... }
function getSeniorityLabel(seniorityId, language) { ... }
function getSeniorityRangeLabel(experienceId, language) { ... }
function getRiskLabel(riskKey, language) { ... }
function buildLocalizedRoast(result, language) { ... }
```

Implementation rules:
- `buildLocalizedRoast(result, language)` must return an empty string if `result`, `result.occupation`, `result.seniority`, or `result.risk` is missing.
- `buildLocalizedRoast(result, language)` must compose `ROAST_LIBRARY[language].openers[result.occupation.id]`, `middles[result.risk.key]`, and `closers[result.seniority.id]`.
- Do not remove or rewrite `result.roast` inside `buildResultModel()` in this plan; later plans will stop rendering from it.
- Do not add `window`, `document`, or `localStorage` access inside `ENGINE`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const html=fs.readFileSync('index.html','utf8');['const DEFAULT_LANGUAGE = \\'en\\'','const LANGUAGE_STORAGE_KEY = \\'jobDeathClockLanguage\\'','const SUPPORTED_LANGUAGES = [\\'en\\', \\'ptbr\\']','const OCCUPATION_LABELS =','const SENIORITY_LABELS =','const SENIORITY_RANGE_LABELS =','const RISK_LABELS =','const ROAST_LIBRARY =','function buildLocalizedRoast(result, language)','software-developers','years-10-plus','critical:'].forEach((snippet)=>{if(!html.includes(snippet)) throw new Error('missing '+snippet);}); console.log('phase 4 catalogs present');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const LANGUAGE_STORAGE_KEY = 'jobDeathClockLanguage'" index.html` returns 1
    - `grep -c 'const OCCUPATION_LABELS =' index.html` returns 1
    - `grep -c 'const SENIORITY_LABELS =' index.html` returns 1
    - `grep -c 'const SENIORITY_RANGE_LABELS =' index.html` returns 1
    - `grep -c 'const RISK_LABELS =' index.html` returns 1
    - `grep -c 'const ROAST_LIBRARY =' index.html` returns 1
    - `grep -c 'function buildLocalizedRoast(result, language)' index.html` returns 1
    - `grep -c 'software-developers' index.html` returns at least 2 within locale maps
    - `grep -c 'years-10-plus' index.html` returns at least 2 within locale maps
  </acceptance_criteria>
  <done>`index.html` contains a stable bilingual content model keyed by ids, and roast localization is now possible without changing the pure engine</done>
</task>

<task type="auto">
  <name>Task 2: Add static social preview metadata and create the Phase 4 verifier plus roast artifact</name>
  <files>index.html, scripts/verify-phase-4-i18n-share.js, .planning/phases/04-content-i18n-and-sharing/04-roast-matrix.json, og-preview.png</files>
  <read_first>
    - index.html
    - scripts/verify-phase-2-results.js
    - .planning/phases/04-content-i18n-and-sharing/04-RESEARCH.md
  </read_first>
  <action>
Continue editing `index.html` and create `scripts/verify-phase-4-i18n-share.js`.

In `index.html <head>`, add these exact static tags using the public GitHub Pages URL:

```html
<meta property="og:type" content="website">
<meta property="og:title" content="AI Job Death Clock">
<meta property="og:description" content="Find out when the machines come for your job. Bilingual cyberpunk countdown, roast, and shareable AI doom.">
<meta property="og:url" content="https://giuice.github.io/risk-jobs/">
<meta property="og:image" content="https://giuice.github.io/risk-jobs/og-preview.png">
<meta name="twitter:card" content="summary">
<meta name="twitter:title" content="AI Job Death Clock">
<meta name="twitter:description" content="Find out when the machines come for your job. Bilingual cyberpunk countdown, roast, and shareable AI doom.">
<meta name="twitter:image" content="https://giuice.github.io/risk-jobs/og-preview.png">
```

Create a repo-root `og-preview.png` at exactly `1200x630` pixels. Use the existing cyberpunk design language from the app:
- dark background (`--bg-primary` family)
- neon green/cyan/pink accents
- title text `AI Job Death Clock`
- one short subtitle about bilingual AI doom/shareability

The image must be a committed local asset served by GitHub Pages. Do not point `og:image` or `twitter:image` at an external third-party host.

Then create `scripts/verify-phase-4-i18n-share.js` using only Node built-ins (`fs`, `path`, `vm`, `assert/strict`) with these exact behaviors:

1. Read `index.html`.
2. Extract the `DATA`, `ENGINE`, and `I18N` blocks with regex.
3. Evaluate them in a `vm` sandbox and export:
   - `OCCUPATIONS`
   - `SENIORITY_LEVELS`
   - `buildResultModel`
   - `SUPPORTED_LANGUAGES`
   - `STRINGS`
   - `OCCUPATION_LABELS`
   - `SENIORITY_LABELS`
   - `SENIORITY_RANGE_LABELS`
   - `RISK_LABELS`
   - `ROAST_LIBRARY`
   - `buildLocalizedRoast`
4. Assert:
   - `SUPPORTED_LANGUAGES` is exactly `['en', 'ptbr']`
   - every locale has all required UI string keys
   - every occupation id exists in `OCCUPATION_LABELS.en` and `.ptbr`
   - every seniority id exists in `SENIORITY_LABELS` for both locales
   - every experience id exists in `SENIORITY_RANGE_LABELS` for both locales
   - every risk key exists in `RISK_LABELS` for both locales
   - for all 84 `buildResultModel(occupation.id, seniority.experienceId)` combinations, `buildLocalizedRoast(result, 'en')` and `buildLocalizedRoast(result, 'ptbr')` are non-empty strings
   - the HTML contains the exact `og:*` and `twitter:*` tags listed above
   - `og-preview.png` exists in the repo root
5. Write `.planning/phases/04-content-i18n-and-sharing/04-roast-matrix.json` with:

```json
{
  "languages": ["en", "ptbr"],
  "occupationCount": 21,
  "seniorityCount": 4,
  "matrixSize": 84,
  "rows": []
}
```

Each row must include:
- `occupationId`
- `seniorityId`
- `riskKey`
- `englishRoast`
- `portugueseRoast`

6. Print one success line beginning exactly with `Phase 4 i18n/share verified:`

Finally run:

```bash
node scripts/verify-phase-2-results.js
node scripts/verify-phase-4-i18n-share.js
```

Do not introduce any npm dependency, package.json change, or browser-only testing framework.
  </action>
  <verify>
    <automated>node scripts/verify-phase-2-results.js && node scripts/verify-phase-4-i18n-share.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'meta property="og:title"' index.html` returns at least 1
    - `grep -c 'meta property="og:description"' index.html` returns at least 1
    - `grep -c 'meta property="og:image"' index.html` returns at least 1
    - `grep -c 'meta name="twitter:card"' index.html` returns at least 1
    - `grep -c 'meta name="twitter:image"' index.html` returns at least 1
    - `test -f og-preview.png` exits 0
    - `test -f scripts/verify-phase-4-i18n-share.js` exits 0
    - `grep -c 'Phase 4 i18n/share verified:' scripts/verify-phase-4-i18n-share.js` returns at least 1
    - `test -f .planning/phases/04-content-i18n-and-sharing/04-roast-matrix.json` exits 0
    - `grep -c '"matrixSize": 84' .planning/phases/04-content-i18n-and-sharing/04-roast-matrix.json` returns at least 1
    - `grep -c '"languages": \\[' .planning/phases/04-content-i18n-and-sharing/04-roast-matrix.json` returns at least 1
    - `node scripts/verify-phase-2-results.js` exits 0
    - `node scripts/verify-phase-4-i18n-share.js` exits 0
  </acceptance_criteria>
  <done>The repo now has launch-ready bilingual content metadata plus a deterministic verifier and roast artifact that later plans can extend</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/verify-phase-2-results.js` exits 0
- [ ] `node scripts/verify-phase-4-i18n-share.js` exits 0
- [ ] `.planning/phases/04-content-i18n-and-sharing/04-roast-matrix.json` exists with `matrixSize: 84`
- [ ] `index.html` contains static `og:*` and `twitter:*` tags
- [ ] `og-preview.png` exists at repo root
</verification>

<success_criteria>
- Bilingual catalogs cover all current UI copy and roast composition pieces
- Localized roast output exists for every occupation/seniority combination in both languages
- Phase 2 engine verification still passes unchanged
- Phase 4 gains a fast automated verifier before any UI/share wiring begins
- Shared links have a committed preview image instead of metadata-only fallbacks
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-i18n-and-sharing/04-01-SUMMARY.md`
</output>
