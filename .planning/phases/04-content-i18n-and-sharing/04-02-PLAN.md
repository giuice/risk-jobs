---
phase: 04-content-i18n-and-sharing
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [index.html, scripts/verify-phase-4-i18n-share.js]
autonomous: true
requirements: [I18N-01, I18N-02, I18N-04]

must_haves:
  truths:
    - "A visible language toggle switches every rendered UI label between English and PT-BR instantly with no page reload"
    - "The selected language persists across reloads via localStorage and also updates the active result view in place"
    - "Re-rendering localized cards and buttons preserves selected profession, selected experience, and CTA enabled state"
    - "The document title and `<html lang>` attribute stay synchronized with the current language"
  artifacts:
    - path: "index.html"
      provides: "Language toggle UI, applyLanguage render flow, and localStorage-backed locale persistence"
      contains: "function applyLanguage(language)"
      min_lines: 980
    - path: "scripts/verify-phase-4-i18n-share.js"
      provides: "Phase 4 verifier extended to assert toggle and persistence wiring"
      contains: "applyLanguage"
      min_lines: 150
  key_links:
    - from: "index.html (.lang-btn buttons)"
      to: "applyLanguage(language)"
      via: "click handlers read data-lang and update locale state"
      pattern: "data-lang|applyLanguage"
    - from: "applyLanguage(language)"
      to: "renderCards/renderExperienceSelector/renderResultContent"
      via: "full rerender path updates input and results views without reload"
      pattern: "renderCards\\(|renderExperienceSelector\\(|renderResultContent\\("
    - from: "localStorage"
      to: "init()"
      via: "initial language resolution uses stored preference before first interactive render"
      pattern: "localStorage\\.getItem\\(LANGUAGE_STORAGE_KEY\\)"
---

<objective>
Wire the bilingual content model into the live interface: a real language toggle, instant UI rerendering, and persisted language preference across reloads.

Purpose: Phase 4 is only valuable if the app feels natively bilingual, not if translated data exists but the page still behaves like a hard-coded English UI.
Output: `index.html` renders in EN or PT-BR on demand, persists the chosen language, and keeps current selections/results intact while switching.
</objective>

<execution_context>
@/home/giuice/.codex/get-shit-done/workflows/execute-plan.md
@/home/giuice/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-content-i18n-and-sharing/04-RESEARCH.md
@.planning/phases/04-content-i18n-and-sharing/04-VALIDATION.md
@.planning/phases/04-content-i18n-and-sharing/04-01-PLAN.md
@index.html
@scripts/verify-phase-4-i18n-share.js

<interfaces>
This plan assumes Plan 01 added:

- `LANGUAGE_STORAGE_KEY`
- locale catalogs and label maps
- `buildLocalizedRoast(result, language)`
- `scripts/verify-phase-4-i18n-share.js`

Current UI state variables already present in `index.html`:

```js
let selectedOccupation = null;
let selectedExperience = null;
let lastResult = null;
```

This plan should add `currentLanguage` and localized render helpers without moving DOM work into `ENGINE`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a shared language toggle and localized render pipeline</name>
  <files>index.html</files>
  <read_first>
    - index.html
    - .planning/phases/04-content-i18n-and-sharing/04-RESEARCH.md
    - .planning/phases/04-content-i18n-and-sharing/04-01-PLAN.md
  </read_first>
  <action>
Edit `index.html` to add one shared language-toggle control and convert all rendered UI text to use the new locale helpers.

Make these exact structural changes:

1. Add a shared toggle block immediately inside `<body>` before `#input-view`:

```html
<div class="language-toggle" id="language-toggle" aria-label="Language selector">
  <span class="language-toggle-label" id="language-toggle-label">Language</span>
  <button type="button" class="lang-btn" data-lang="en">EN</button>
  <button type="button" class="lang-btn" data-lang="ptbr">PT-BR</button>
</div>
```

2. Add ids to every currently hard-coded visible label so they can be updated without brittle selector text matching:
   - `input-title`
   - `input-subtitle`
   - `profession-label`
   - `experience-label`
   - `results-title`
   - `countdown-heading`
   - `gauge-heading`
   - `roast-heading`

3. Add CSS for:
   - `.language-toggle`
   - `.language-toggle-label`
   - `.lang-btn`
   - `.lang-btn.is-active`

Use the existing cyberpunk token system:
- inactive border/text: `var(--text-dim)`
- active border/text: `var(--neon-cyan)`
- active glow: `var(--glow-cyan)`

4. Add these exact functions in `// ===== UI =====`:

```js
let currentLanguage = DEFAULT_LANGUAGE;

function renderStaticLabels() { ... }
function renderResultContent(result) { ... }
function syncLanguageControls() { ... }
function applyLanguage(language) { ... }
```

Implementation rules:
- `renderStaticLabels()` must set all title, heading, label, button, and gauge-edge text from `STRINGS`
- `renderCards()` must be updated to use `getOccupationLabel(occ.id, currentLanguage)` and `t('riskPrefix', currentLanguage)`
- `renderExperienceSelector()` must use `getSeniorityLabel(level.id, currentLanguage)` and `getSeniorityRangeLabel(level.experienceId, currentLanguage)`
- `renderResultContent(result)` must render:
  - localized occupation label
  - localized seniority label
  - localized risk label
  - localized roast via `buildLocalizedRoast(result, currentLanguage)`
- `showResults(result)` must call `renderResultContent(result)` instead of reading `result.roast`
- `applyLanguage(language)` must:
  - normalize unsupported values to `DEFAULT_LANGUAGE`
  - set `currentLanguage`
  - call `localStorage.setItem(LANGUAGE_STORAGE_KEY, currentLanguage)`
  - set `document.documentElement.lang` to `'pt-BR'` for `ptbr`, else `'en'`
  - set `document.title = t('documentTitle', currentLanguage)`
  - rerender cards, experience selector, static labels, and active result content
  - restore `.selected` styling and CTA enabled state after rerender
  - update `.lang-btn.is-active`

Do not use `innerHTML` to rebuild the entire page. Re-render only the existing card/selector lists and targeted label nodes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const html=fs.readFileSync('index.html','utf8');['id=\"language-toggle\"','data-lang=\"en\"','data-lang=\"ptbr\"','function renderStaticLabels()','function renderResultContent(result)','function syncLanguageControls()','function applyLanguage(language)','localStorage.setItem(LANGUAGE_STORAGE_KEY, currentLanguage)','document.documentElement.lang = currentLanguage === \\'ptbr\\' ? \\'pt-BR\\' : \\'en\\'','document.title = t(\\'documentTitle\\', currentLanguage)','buildLocalizedRoast(result, currentLanguage)'].forEach((snippet)=>{if(!html.includes(snippet)) throw new Error('missing '+snippet);}); console.log('phase 4 language UI wiring present');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="language-toggle"' index.html` returns 1
    - `grep -c 'data-lang="en"' index.html` returns at least 1
    - `grep -c 'data-lang="ptbr"' index.html` returns at least 1
    - `grep -c 'function applyLanguage(language)' index.html` returns 1
    - `grep -c 'function renderResultContent(result)' index.html` returns 1
    - `grep -c 'buildLocalizedRoast(result, currentLanguage)' index.html` returns at least 1
    - `grep -c 'document.title = t('\''documentTitle'\'', currentLanguage)' index.html` returns at least 1
    - `grep -c 'document.documentElement.lang = currentLanguage === '\''ptbr'\'' ? '\''pt-BR'\'' : '\''en'\''' index.html` returns at least 1
  </acceptance_criteria>
  <done>The interface has a real bilingual toggle and a single rerender path that updates all visible labels and results without a reload</done>
</task>

<task type="auto">
  <name>Task 2: Persist the selected language and extend the verifier for UI wiring checks</name>
  <files>index.html, scripts/verify-phase-4-i18n-share.js</files>
  <read_first>
    - index.html
    - scripts/verify-phase-4-i18n-share.js
    - .planning/phases/04-content-i18n-and-sharing/04-VALIDATION.md
  </read_first>
  <action>
Continue editing `index.html` and extend `scripts/verify-phase-4-i18n-share.js`.

In `index.html`, add `resolveInitialLanguage(parsedHash)` with this exact order:

1. If `parsedHash.lang` is supported, return it.
2. Else if `localStorage.getItem(LANGUAGE_STORAGE_KEY)` is supported, return it.
3. Else return `DEFAULT_LANGUAGE`.

Update `init()` so that before first interactive render it:

1. determines the initial language via `resolveInitialLanguage({ lang: '' })` for now
2. assigns `currentLanguage`
3. calls `renderCards()`
4. calls `renderExperienceSelector()`
5. calls `renderStaticLabels()`
6. calls `syncLanguageControls()`
7. binds click listeners on `.lang-btn` to call `applyLanguage(button.dataset.lang)`

Rules:
- do not lose `selectedOccupation` or `selectedExperience` when the language changes
- after rerender, call `updateCTA()`
- if `lastResult` exists during a language switch, update the displayed result in place instead of clearing it

Extend `scripts/verify-phase-4-i18n-share.js` so it also asserts:

- `function resolveInitialLanguage(parsedHash)` exists
- `function applyLanguage(language)` exists
- `localStorage.setItem(LANGUAGE_STORAGE_KEY, currentLanguage)` exists in `index.html`
- `localStorage.getItem(LANGUAGE_STORAGE_KEY)` exists in `index.html`
- `document.documentElement.lang` is updated
- `renderCards()` and `renderExperienceSelector()` are called from `applyLanguage` or the initial boot flow
- `buildLocalizedRoast(` appears in result rendering code

Run:

```bash
node scripts/verify-phase-2-results.js
node scripts/verify-phase-4-i18n-share.js
```
  </action>
  <verify>
    <automated>node scripts/verify-phase-2-results.js && node scripts/verify-phase-4-i18n-share.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'function resolveInitialLanguage(parsedHash)' index.html` returns 1
    - `grep -c 'localStorage.getItem(LANGUAGE_STORAGE_KEY)' index.html` returns at least 1
    - `grep -c 'localStorage.setItem(LANGUAGE_STORAGE_KEY, currentLanguage)' index.html` returns at least 1
    - `grep -c 'renderCards();' index.html` returns at least 1 within `applyLanguage` or boot flow
    - `grep -c 'renderExperienceSelector();' index.html` returns at least 1 within `applyLanguage` or boot flow
    - `grep -c 'updateCTA();' index.html` returns at least 1 within `applyLanguage`
    - `node scripts/verify-phase-2-results.js` exits 0
    - `node scripts/verify-phase-4-i18n-share.js` exits 0
  </acceptance_criteria>
  <done>Language selection persists across reloads, updates the live interface, and is covered by the Phase 4 verifier</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/verify-phase-2-results.js` exits 0
- [ ] `node scripts/verify-phase-4-i18n-share.js` exits 0
- [ ] Switching language updates input and result labels without clearing valid current state
- [ ] `localStorage` persistence is wired through `LANGUAGE_STORAGE_KEY`
</verification>

<success_criteria>
- Clicking the language toggle swaps visible copy between EN and PT-BR with no reload
- Language choice persists through reloads
- Current selections and results survive rerenders cleanly
- Document title and `<html lang>` stay accurate
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-i18n-and-sharing/04-02-SUMMARY.md`
</output>
