---
phase: 05-ux-polish-missing-professions-calculation-explainer-modal-and-article-link
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - index.html
autonomous: true
requirements:
  - CARD-UX
  - MODAL
  - ARTICLE
  - SHARE-FB-IG

must_haves:
  truths:
    - "Selection cards no longer show a risk score badge — the suspense is preserved until results reveal"
    - "A 'HOW IS THIS CALCULATED?' button appears on the results view"
    - "Clicking the button opens a futuristic modal dialog explaining the formula in plain language and scientific terms"
    - "Modal closes via the X button, Escape key, or clicking the backdrop"
    - "Modal content switches language when the language toggle is used"
    - "An article link to post-labor-economia-ptbr.html appears on the results view with bilingual label"
    - "Facebook and Instagram share buttons appear alongside the existing X, WhatsApp, LinkedIn buttons"
    - "All new HTML elements are present in the DOM (verifier exits 0)"
  artifacts:
    - path: "index.html"
      provides: "results-view with modal, article link, FB/IG share buttons; no card-risk in renderCards()"
      contains: "explainer-modal"
    - path: "index.html"
      provides: "STRINGS with explainerModalTitle keys in en and ptbr"
      contains: "explainerModalTitle"
  key_links:
    - from: "#explainer-trigger-btn"
      to: "#explainer-modal"
      via: "showModal() in BOOT event listener"
      pattern: "showModal"
    - from: "applyLanguage()"
      to: "renderModalContent()"
      via: "called inside applyLanguage after renderStaticLabels()"
      pattern: "renderModalContent"
    - from: "#share-facebook-btn"
      to: "buildFacebookShareUrl()"
      via: "click event listener in BOOT"
      pattern: "share-facebook-btn"
---

<objective>
Apply four UI polish changes to index.html: remove the risk score spoiler from selection cards, add the calculation explainer modal, add an article link on the results view, and add Facebook and Instagram sharing buttons.

Purpose: These changes complete the UX promise — suspense is preserved until reveal, curious users can understand the formula, Brazilian users get a direct link to the translated article, and the share surface area expands to the two platforms where sharing behavior is most impulsive (Facebook) and visual (Instagram).

Output: Updated index.html with: no `.card-risk` nodes in `renderCards()`, a `<dialog id="explainer-modal">` element, `#article-link` with correct href, `#share-facebook-btn` and `#share-instagram-btn` with working share URLs, and all STRINGS keys for modal content in both languages.
</objective>

<execution_context>
@/home/giuice/.claude/get-shit-done/workflows/execute-plan.md
@/home/giuice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ux-polish-missing-professions-calculation-explainer-modal-and-article-link/05-RESEARCH.md
@.planning/phases/05-ux-polish-missing-professions-calculation-explainer-modal-and-article-link/05-01-SUMMARY.md

<interfaces>
<!-- Exact locations in index.html the executor will edit. -->

**Results view HTML (lines 669-732) — insertion points:**
```html
<div id="results-view">
  <div class="container">
    <!-- ... countdown, gauge, roast cards ... -->
    <section class="share-actions" id="share-actions">
      <p class="results-card-heading" id="share-heading">Share This Doom</p>
      <div class="share-actions-row">
        <button type="button" class="share-btn" id="share-x-btn">X / Twitter</button>
        <button type="button" class="share-btn" id="share-whatsapp-btn">WhatsApp</button>
        <button type="button" class="share-btn" id="share-linkedin-btn">LinkedIn</button>
        <!-- ADD: share-facebook-btn and share-instagram-btn here -->
      </div>
    </section>
    <!-- ADD: explainer-trigger-container here -->
    <!-- ADD: article-link-container here -->
    <div class="try-again-container">
      <button class="try-again-btn" id="try-again-btn">&#x21BB; TRY AGAIN</button>
    </div>
  </div>
</div>
<!-- ADD: <dialog id="explainer-modal"> before </body> -->
```

**renderCards() in UI section (lines 1264-1298):**
```javascript
function renderCards() {
  const grid = document.getElementById('card-grid');
  grid.replaceChildren();
  OCCUPATIONS.forEach(function(occ) {
    // ...
    const cardName = document.createElement('div');
    cardName.className = 'card-name';
    cardName.textContent = getOccupationLabel(occ.id, currentLanguage);

    const cardRisk = document.createElement('div');         // REMOVE these 4 lines
    cardRisk.className = 'card-risk';                       // REMOVE
    cardRisk.textContent = t('riskPrefix', ...) + ...;     // REMOVE
                                                            // REMOVE

    card.appendChild(cardName);
    card.appendChild(cardRisk);   // REMOVE this line too
    // ...
  });
}
```

**renderStaticLabels() in UI section (lines 1191-1215) — where to add modal/article label updates:**
```javascript
function renderStaticLabels() {
  // ... existing lines ...
  document.getElementById('try-again-btn').textContent = t('tryAgainLabel', currentLanguage);
  // ADD:
  // document.getElementById('explainer-trigger-btn').textContent = t('explainerTriggerLabel', currentLanguage);
  // document.getElementById('article-link').textContent = t('articleLinkLabel', currentLanguage);
  // document.getElementById('share-facebook-btn').textContent = t('shareFacebookLabel', currentLanguage);
  // document.getElementById('share-instagram-btn').textContent = t('shareInstagramLabel', currentLanguage);
}
```

**applyLanguage() in UI section (lines 1241-1258):**
```javascript
function applyLanguage(language) {
  // ...
  renderCards();
  renderExperienceSelector();
  renderStaticLabels();
  // ADD: renderModalContent(currentLanguage);
  // ...
}
```

**STRINGS object (lines 906-959) — add these keys to both en and ptbr blocks:**
New keys needed: `explainerTriggerLabel`, `explainerModalTitle`, `explainerModalBodyHtml`, `articleLinkLabel`, `shareFacebookLabel`, `shareInstagramLabel`

**BOOT section init() (lines 1542-1588) — add event listeners:**
```javascript
// ADD after LinkedIn listener:
document.getElementById('share-facebook-btn').addEventListener('click', function() { ... });
document.getElementById('share-instagram-btn').addEventListener('click', function() { ... });
document.getElementById('explainer-trigger-btn').addEventListener('click', function() {
  document.getElementById('explainer-modal').showModal();
});
document.getElementById('modal-close-btn').addEventListener('click', function() {
  document.getElementById('explainer-modal').close();
});
document.getElementById('explainer-modal').addEventListener('click', function(e) {
  if (e.target === this) this.close();
});
```

**CSS variables already established (existing in <style>):**
`--neon-cyan`, `--neon-green`, `--neon-pink`, `--bg-secondary`, `--text-primary`, `--font-mono`, `--border-radius`, `--glow-cyan`

**body::after scanline z-index:** `z-index: 9999` — dialog via showModal() renders in top layer above this, so no z-index conflict needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove card-risk from renderCards() and add STRINGS keys for new UI elements</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 110-125 (CSS for .card-risk — find the rule to delete)
    - index.html lines 1264-1298 (renderCards() — exact lines to remove)
    - index.html lines 906-959 (STRINGS object — where to add new keys)
    - index.html lines 1191-1215 (renderStaticLabels() — where to add new getElementById calls)
    - index.html lines 1241-1258 (applyLanguage() — where to add renderModalContent call)
  </read_first>
  <action>
Make these changes to index.html:

**A. Remove card-risk CSS rule** (find and delete the block):
```css
.card .card-risk {
  /* whatever is here */
}
```
Also delete any `.card-risk` rule in the responsive section (search for `card-risk` in CSS area).

**B. Remove card-risk DOM creation from renderCards()** (lines ~1281-1286):
Delete these 5 lines from renderCards():
```javascript
const cardRisk = document.createElement('div');
cardRisk.className = 'card-risk';
cardRisk.textContent = t('riskPrefix', currentLanguage) + ': ' + occ.baseScore + '/10';
card.appendChild(cardRisk);
```
(Keep `card.appendChild(cardName)` — only remove the cardRisk lines.)

**C. Add new string keys to STRINGS.en** (after `languagePortuguese: 'Portuguese (BR)'`, before the closing `}`):
```javascript
explainerTriggerLabel: 'HOW IS THIS CALCULATED?',
explainerModalTitle: 'How This Calculation Works',
explainerModalBodyHtml: '<div class="modal-section"><h3>For Everyone</h3><p>We use data from AI researcher Andrej Karpathy\'s analysis of hundreds of US occupations to give each job a "Base AI Exposure Score" from 0 to 10. Jobs done entirely on a computer score high. Jobs requiring physical presence score low.</p></div><div class="modal-section"><h3>The Formula</h3><ol><li><strong>Base Score (0–10):</strong> How exposed is your job type to AI automation?</li><li><strong>Seniority Adjustment:</strong> −1.5 points per seniority level (Junior=0, Mid=−1.5, Senior=−3, Architect=−4.5). Adjusted Score = Base − (Level × 1.5), clamped to 0–10.</li><li><strong>Shelf Life:</strong> (10 − Adjusted Score) × 1.2, max 12 years. Score 10 = 0 years. Score 0 = 12 years.</li></ol></div><div class="modal-section"><h3>Risk Bands</h3><ul><li><strong>LOW RISK (0–2.5):</strong> Machines only have you bookmarked.</li><li><strong>WATCHLIST (2.5–4.5):</strong> Replacement memo drafted, awaiting budget.</li><li><strong>EXPOSED (4.5–6.5):</strong> No longer future-proof.</li><li><strong>CRITICAL (6.5–8.5):</strong> Automation pilot has your chair in its rollout plan.</li><li><strong>DOOMED (8.5–10):</strong> AI is just asking where you keep the passwords.</li></ul></div><div class="modal-section modal-section--science"><h3>For Scientists</h3><p>This is a heuristic model, not a rigorous economic forecast. Scores are LLM-estimated against BLS Occupational Outlook data. The seniority modifier is a linear penalty reflecting human capital as a partial moat. No model predicts the future — this is a thinking tool, not a prophecy.</p></div>',
articleLinkLabel: 'Read: The 7 Frameworks for Understanding the Post-Labor Economy (PT-BR)',
shareFacebookLabel: 'Share on Facebook',
shareInstagramLabel: 'Copy for Instagram',
```

**D. Add new string keys to STRINGS.ptbr** (after `languagePortuguese: 'Portugues (BR)'`, before the closing `}`):
```javascript
explainerTriggerLabel: 'COMO ISSO E CALCULADO?',
explainerModalTitle: 'Como Este Calculo Funciona',
explainerModalBodyHtml: '<div class="modal-section"><h3>Para Todo Mundo</h3><p>Usamos dados da analise do pesquisador de IA Andrej Karpathy sobre centenas de ocupacoes para dar a cada profissao uma "Pontuacao Base de Exposicao a IA" de 0 a 10. Trabalhos feitos inteiramente no computador pontuam alto. Trabalhos que exigem presenca fisica pontuam baixo.</p></div><div class="modal-section"><h3>A Formula</h3><ol><li><strong>Pontuacao Base (0–10):</strong> Qual a exposicao da sua profissao a automacao por IA?</li><li><strong>Ajuste de Senioridade:</strong> −1,5 pontos por nivel (Junior=0, Mid=−1,5, Senior=−3, Arquiteto=−4,5). Pontuacao Ajustada = Base − (Nivel × 1,5), limitado entre 0–10.</li><li><strong>Prazo de Validade:</strong> (10 − Pontuacao Ajustada) × 1,2, maximo 12 anos. Pontuacao 10 = 0 anos. Pontuacao 0 = 12 anos.</li></ol></div><div class="modal-section"><h3>Faixas de Risco</h3><ul><li><strong>BAIXO RISCO (0–2,5):</strong> As maquinas so deixaram voce nos favoritos.</li><li><strong>VIGIADO (2,5–4,5):</strong> Memorando de substituicao rascunhado, aguardando budget.</li><li><strong>EXPOSTO (4,5–6,5):</strong> Voce ja nao esta protegido para o futuro.</li><li><strong>CRITICO (6,5–8,5):</strong> O piloto de automacao reservou sua cadeira no cronograma.</li><li><strong>CONDENADO (8,5–10):</strong> A IA so quer saber onde ficam as senhas.</li></ul></div><div class="modal-section modal-section--science"><h3>Para Cientistas</h3><p>Este e um modelo heuristico, nao uma previsao economica rigorosa. As pontuacoes foram estimadas por LLM contra dados do BLS Occupational Outlook. O modificador de senioridade e uma penalidade linear que reflete capital humano como barreira parcial. Nenhum modelo preve o futuro — esta e uma ferramenta de reflexao, nao uma profecia.</p></div>',
articleLinkLabel: 'Leia: Os 7 Frameworks para Entender a Economia Pos-Trabalho',
shareFacebookLabel: 'Compartilhar no Facebook',
shareInstagramLabel: 'Copiar para Instagram',
```

**E. Add renderModalContent() function in UI section** (after renderStaticLabels() function, before renderResultContent()):
```javascript
function renderModalContent(language) {
  const titleEl = document.getElementById('modal-title-heading');
  const bodyEl = document.getElementById('modal-body');
  if (titleEl) titleEl.textContent = t('explainerModalTitle', language);
  if (bodyEl) bodyEl.innerHTML = t('explainerModalBodyHtml', language);
}
```

**F. Update renderStaticLabels()** — add these lines before the closing `}`:
```javascript
document.getElementById('explainer-trigger-btn').textContent = t('explainerTriggerLabel', currentLanguage);
document.getElementById('article-link').textContent = t('articleLinkLabel', currentLanguage);
document.getElementById('share-facebook-btn').textContent = t('shareFacebookLabel', currentLanguage);
document.getElementById('share-instagram-btn').textContent = t('shareInstagramLabel', currentLanguage);
renderModalContent(currentLanguage);
```

**G. Add SHARE functions** in SHARE section (after buildLinkedInShareUrl):
```javascript
function buildFacebookShareUrl(result) {
  return 'https://www.facebook.com/sharer/sharer.php?u=' +
    encodeURIComponent(buildStateUrl(buildShareState(result)));
}

function buildInstagramCopyText(result, language) {
  return buildShareMessage(result, language);
}
```

Note: Instagram has no web share API. The Instagram "share" button copies the share text to clipboard so the user can paste it into an Instagram story/post. Show a brief notification.
  </action>
  <verify>
    <automated>grep -c "card-risk" index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "card-risk" index.html` outputs 0 (all card-risk references removed from CSS and JS)
    - `grep "explainerTriggerLabel" index.html` returns matches in both en and ptbr STRINGS blocks
    - `grep "explainerModalTitle" index.html` returns matches in both en and ptbr STRINGS blocks
    - `grep "explainerModalBodyHtml" index.html` returns matches in both en and ptbr STRINGS blocks
    - `grep "articleLinkLabel" index.html` returns matches in both en and ptbr STRINGS blocks
    - `grep "shareFacebookLabel" index.html` returns matches in both en and ptbr STRINGS blocks
    - `grep "renderModalContent" index.html` returns at least 3 matches (function definition + call in renderStaticLabels + call in applyLanguage if added)
    - `grep "buildFacebookShareUrl" index.html` returns at least 1 match
  </acceptance_criteria>
  <done>No card-risk CSS rule or DOM element creation remains in index.html, STRINGS.en and STRINGS.ptbr contain all 6 new keys (explainerTriggerLabel, explainerModalTitle, explainerModalBodyHtml, articleLinkLabel, shareFacebookLabel, shareInstagramLabel), renderModalContent() function defined, buildFacebookShareUrl() and buildInstagramCopyText() defined in SHARE section.</done>
</task>

<task type="auto">
  <name>Task 2: Add modal HTML, article link, FB/IG share buttons, CSS, and BOOT event listeners</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 669-732 (results-view HTML — exact insertion points for new elements)
    - index.html lines 1525-1588 (BOOT section / init() — where to add event listeners)
    - index.html lines 100-135 (CSS area near .card-risk — confirm it is now gone, find a good spot for modal CSS)
    - index.html lines 420-495 (.try-again-container, .share-actions CSS — understand surrounding styles)
    - .planning/phases/05-ux-polish-missing-professions-calculation-explainer-modal-and-article-link/05-RESEARCH.md lines 63-120 (exact modal HTML and CSS patterns)
  </read_first>
  <action>
Make these changes to index.html:

**A. Add Facebook and Instagram share buttons to share-actions-row** (inside the existing `<div class="share-actions-row">`):
Add after `<button type="button" class="share-btn" id="share-linkedin-btn">`:
```html
<button type="button" class="share-btn" id="share-facebook-btn">Share on Facebook</button>
<button type="button" class="share-btn" id="share-instagram-btn">Copy for Instagram</button>
```

**B. Add explainer trigger button** — insert after `</section>` (closing of share-actions) and before `<div class="try-again-container">`:
```html
<div class="explainer-trigger-container">
  <button type="button" class="explainer-trigger-btn" id="explainer-trigger-btn">HOW IS THIS CALCULATED?</button>
</div>
```

**C. Add article link** — insert after the explainer-trigger-container and before `<div class="try-again-container">`:
```html
<div class="article-link-container" id="article-link-container">
  <a href="post-labor-economia-ptbr.html"
     class="article-link"
     id="article-link"
     target="_blank"
     rel="noopener noreferrer"></a>
</div>
```
(textContent is set by renderStaticLabels() from Task 1.)

**D. Add modal dialog** — insert before `</body>`:
```html
<dialog id="explainer-modal" class="explainer-modal" aria-labelledby="modal-title-heading">
  <div class="explainer-modal-inner">
    <div class="modal-header">
      <h2 class="modal-title" id="modal-title-heading"></h2>
      <button class="modal-close-btn" id="modal-close-btn" type="button" aria-label="Close">&times;</button>
    </div>
    <div class="modal-body" id="modal-body"></div>
  </div>
</dialog>
```

**E. Add CSS** — insert new rules in the `<style>` block (after the existing `.try-again-btn` rules, before the closing `</style>` or before responsive media queries):

```css
/* ---- Explainer trigger button ---- */
.explainer-trigger-container {
  text-align: center;
  margin: 16px 0 8px;
}
.explainer-trigger-btn {
  background: transparent;
  border: 1px solid var(--neon-cyan);
  color: var(--neon-cyan);
  font-family: var(--font-mono);
  font-size: 0.75rem;
  letter-spacing: 0.1em;
  padding: 8px 20px;
  cursor: pointer;
  transition: background 0.2s, box-shadow 0.2s;
}
.explainer-trigger-btn:hover,
.explainer-trigger-btn:focus-visible {
  background: rgba(0, 212, 255, 0.08);
  box-shadow: var(--glow-cyan);
  outline: none;
}

/* ---- Article link ---- */
.article-link-container {
  text-align: center;
  margin: 8px 0 16px;
}
.article-link {
  color: var(--neon-cyan);
  font-family: var(--font-mono);
  font-size: 0.75rem;
  text-decoration: underline;
  text-underline-offset: 3px;
  opacity: 0.8;
  transition: opacity 0.2s;
}
.article-link:hover {
  opacity: 1;
}

/* ---- Explainer modal ---- */
.explainer-modal {
  background: var(--bg-secondary);
  border: 1px solid var(--neon-cyan);
  border-radius: var(--border-radius);
  box-shadow: var(--glow-cyan), 0 0 60px rgba(0, 212, 255, 0.1);
  color: var(--text-primary);
  font-family: var(--font-mono);
  max-width: 700px;
  width: calc(100% - 40px);
  max-height: 80vh;
  overflow-y: auto;
  padding: 0;
  -webkit-overflow-scrolling: touch;
}
.explainer-modal::backdrop {
  background: rgba(0, 0, 0, 0.75);
  backdrop-filter: blur(2px);
}
.explainer-modal-inner {
  padding: 24px;
}
.modal-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 20px;
  gap: 12px;
}
.modal-title {
  color: var(--neon-cyan);
  font-size: 1rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  margin: 0;
}
.modal-close-btn {
  background: transparent;
  border: 1px solid var(--neon-pink);
  color: var(--neon-pink);
  font-size: 1.2rem;
  line-height: 1;
  padding: 4px 10px;
  cursor: pointer;
  flex-shrink: 0;
  font-family: var(--font-mono);
  transition: background 0.2s;
}
.modal-close-btn:hover,
.modal-close-btn:focus-visible {
  background: rgba(255, 0, 128, 0.12);
  outline: none;
}
.modal-body {
  font-size: 0.85rem;
  line-height: 1.7;
  color: var(--text-primary);
}
.modal-section {
  margin-bottom: 20px;
}
.modal-section h3 {
  color: var(--neon-green);
  font-size: 0.8rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  margin: 0 0 8px;
}
.modal-section p,
.modal-section ol,
.modal-section ul {
  margin: 0;
  padding-left: 20px;
}
.modal-section ol,
.modal-section ul {
  padding-left: 20px;
}
.modal-section li {
  margin-bottom: 6px;
}
.modal-section--science {
  border-top: 1px solid rgba(0, 212, 255, 0.2);
  padding-top: 16px;
  opacity: 0.75;
}
@media (prefers-reduced-motion: no-preference) {
  .explainer-modal[open] {
    animation: modal-fade-in 0.15s ease-out;
  }
}
@keyframes modal-fade-in {
  from { opacity: 0; transform: translateY(-8px); }
  to   { opacity: 1; transform: translateY(0); }
}
```

**F. Add event listeners to BOOT init()** — insert after the LinkedIn event listener (after `document.getElementById('share-linkedin-btn').addEventListener(...)`):

```javascript
document.getElementById('share-facebook-btn').addEventListener('click', function() {
  if (!lastResult) return;
  openShareWindow(buildFacebookShareUrl(lastResult));
});

document.getElementById('share-instagram-btn').addEventListener('click', function() {
  if (!lastResult) return;
  const text = buildInstagramCopyText(lastResult, currentLanguage);
  if (navigator.clipboard && navigator.clipboard.writeText) {
    navigator.clipboard.writeText(text).then(function() {
      const btn = document.getElementById('share-instagram-btn');
      const original = btn.textContent;
      btn.textContent = 'COPIED!';
      setTimeout(function() { btn.textContent = original; }, 2000);
    }).catch(function() {
      openShareWindow('https://www.instagram.com/');
    });
  } else {
    openShareWindow('https://www.instagram.com/');
  }
});

document.getElementById('explainer-trigger-btn').addEventListener('click', function() {
  document.getElementById('explainer-modal').showModal();
});

document.getElementById('modal-close-btn').addEventListener('click', function() {
  document.getElementById('explainer-modal').close();
});

document.getElementById('explainer-modal').addEventListener('click', function(e) {
  if (e.target === this) this.close();
});
```
  </action>
  <verify>
    <automated>node scripts/verify-phase-5-ux-polish.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-phase-5-ux-polish.js` exits 0 with message "Phase 5 verification passed. Occupations: 31"
    - `grep 'id="explainer-modal"' index.html` returns 1 match
    - `grep 'id="article-link"' index.html` returns 1 match
    - `grep 'href="post-labor-economia-ptbr.html"' index.html` returns 1 match
    - `grep 'id="share-facebook-btn"' index.html` returns 1 match
    - `grep 'id="share-instagram-btn"' index.html` returns 1 match
    - `grep 'showModal' index.html` returns at least 1 match
    - `grep 'buildFacebookShareUrl' index.html` returns at least 2 matches (function definition + usage)
    - `grep 'renderModalContent' index.html` returns at least 3 matches (definition + renderStaticLabels call + applyLanguage call)
    - Full regression: `node scripts/verify-phase-2-results.js && node scripts/verify-phase-4-i18n-share.js` both exit 0
  </acceptance_criteria>
  <done>node scripts/verify-phase-5-ux-polish.js exits 0, #explainer-modal dialog element in HTML, #article-link with correct href in HTML, #share-facebook-btn and #share-instagram-btn in HTML, showModal() wired in BOOT, full regression suite green.</done>
</task>

</tasks>

<verification>
Full suite after both tasks:
```bash
node scripts/verify-phase-5-ux-polish.js && node scripts/verify-phase-2-results.js && node scripts/verify-phase-4-i18n-share.js
```

All three must exit 0. If phase-2 or phase-4 verifiers fail with "Expected 21 occupations", update the hardcoded count in those scripts.
</verification>

<success_criteria>
- `node scripts/verify-phase-5-ux-polish.js` exits 0 — "Phase 5 verification passed. Occupations: 31"
- No `card-risk` references remain in index.html (CSS or JS)
- `#explainer-modal`, `#article-link`, `#share-facebook-btn`, `#share-instagram-btn` all present in HTML
- Modal content keys exist in both STRINGS.en and STRINGS.ptbr
- SHARE section contains buildFacebookShareUrl() and buildInstagramCopyText()
- BOOT section wires all new button event listeners
- Full regression suite green
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish-missing-professions-calculation-explainer-modal-and-article-link/05-02-SUMMARY.md`
</output>
