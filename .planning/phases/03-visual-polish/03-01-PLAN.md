---
phase: 03-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [VIS-02, VIS-03, VIS-04]

must_haves:
  truths:
    - "Results headings have visible neon glow via multi-layer text-shadow"
    - "A CRT scanline overlay covers the full page without blocking interaction"
    - "Results title performs a one-shot glitch reveal animation when results appear"
    - "All keyframe animations are suppressed under prefers-reduced-motion"
  artifacts:
    - path: "index.html"
      provides: "Neon glow CSS custom properties, scanline overlay, glitch keyframes, reduced motion media query"
      contains: "--neon-glow-green"
  key_links:
    - from: "index.html (showResults function)"
      to: "index.html (.page-title.glitch-active CSS)"
      via: "classList.add('glitch-active') inside requestAnimationFrame"
      pattern: "glitch-active"
    - from: "index.html (resetResultState function)"
      to: "index.html (.page-title.glitch-active CSS)"
      via: "classList.remove('glitch-active')"
      pattern: "classList\\.remove.*glitch-active"
    - from: "index.html (.page-title::before/::after)"
      to: "index.html (.page-title data-text attribute)"
      via: "content: attr(data-text)"
      pattern: "attr\\(data-text\\)"
---

<objective>
Add the three core visual effects — neon text glow, CRT scanline overlay, and glitch reveal animation — to the existing index.html, plus a reduced-motion accessibility guard.

Purpose: These CSS effects transform the results page from functional to screenshot-worthy, directly enabling the viral share loop that is the project's core value.
Output: index.html with neon glow on headings, scanline overlay on body, glitch animation on results title, and prefers-reduced-motion suppression.
</objective>

<execution_context>
@/home/giuice/.claude/get-shit-done/workflows/execute-plan.md
@/home/giuice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-polish/03-RESEARCH.md
@.planning/phases/03-visual-polish/03-UI-SPEC.md

<interfaces>
<!-- Key selectors and functions the executor must work with. Extracted from index.html. -->

From index.html `:root` (lines 11-31) — existing CSS custom properties to extend:
```css
--neon-green: #00ff41;
--neon-cyan: #00d4ff;
--neon-pink: #ff2e97;
--bg-primary: #0a0a0f;
```

From index.html CSS (line 176-183) — .page-title selector already exists:
```css
.page-title {
  text-align: center;
  color: var(--neon-green);
  font-size: 1.8rem;
  margin-bottom: 8px;
  text-transform: uppercase;
  letter-spacing: 2px;
}
```

From index.html CSS (line 260-265) — .countdown-value selector already exists:
```css
.countdown-value {
  font-size: 3rem;
  color: var(--neon-green);
  line-height: 1;
  display: block;
}
```

From index.html CSS (line 232-240) — .results-card-heading selector already exists:
```css
.results-card-heading {
  font-size: 0.75rem;
  color: var(--text-secondary);
  letter-spacing: 2px;
  text-transform: uppercase;
  margin: 0 0 16px 0;
  border-bottom: 1px solid var(--text-dim);
  padding-bottom: 8px;
}
```

From index.html CSS (line 222-226) — existing reveal transition to preserve:
```css
.results-panel.results-visible .results-reveal {
  opacity: 1;
  transform: translateY(0);
  transition: opacity 220ms ease, transform 220ms ease;
}
```

From index.html JS (line 673-696) — showResults function structure:
```javascript
function showResults(result) {
  // ... DOM updates (lines 674-685) ...
  var panel = document.getElementById('results-panel');
  var needle = document.getElementById('gauge-needle');
  panel.classList.remove('results-visible');
  needle.style.transform = 'rotate(-90deg)';
  requestAnimationFrame(function() {
    panel.classList.add('results-visible');
    needle.style.transform = 'rotate(' + result.risk.needleAngle + 'deg)';
  });
}
```

From index.html JS (line 698-711) — resetResultState function structure:
```javascript
function resetResultState() {
  // ... DOM resets ...
  document.getElementById('gauge-needle').style.transform = 'rotate(-90deg)';
}
```

From index.html HTML (line 385) — results view page-title element:
```html
<h1 class="page-title">Your fate is sealed</h1>
```

The `<style>` block ends at line 358. New CSS sections go immediately before `</style>`.
The `showResults` function is at line 673. JS modifications go inside it.
The `resetResultState` function is at line 698. JS modifications go inside it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add neon glow variables and scanline overlay CSS</name>
  <files>index.html</files>
  <read_first>
    - index.html (full file — understand current CSS structure, :root block at lines 11-31, style block ending at line 358)
    - .planning/phases/03-visual-polish/03-RESEARCH.md (Pattern 1: Multi-Layer Neon Text-Shadow, Pattern 2: Scanline Overlay)
    - .planning/phases/03-visual-polish/03-UI-SPEC.md (New tokens table, Interaction Contract VIS-02 and VIS-03)
  </read_first>
  <action>
Add the following CSS sections inside the existing `<style>` block, immediately before the closing `</style>` tag (currently at line 358):

**1. Extend `:root` with three new neon glow text-shadow custom properties.** Add these three new properties inside the existing `:root { ... }` block (after line 30, before the closing `}`):

```css
--neon-glow-green:
  0 0 4px #fff,
  0 0 8px rgba(0,255,65,0.8),
  0 0 16px var(--neon-green),
  0 0 32px var(--neon-green),
  0 0 60px rgba(0,255,65,0.4);
--neon-glow-pink:
  0 0 4px #fff,
  0 0 8px rgba(255,46,151,0.8),
  0 0 16px var(--neon-pink),
  0 0 32px var(--neon-pink);
--neon-glow-cyan:
  0 0 4px #fff,
  0 0 8px rgba(0,212,255,0.8),
  0 0 16px var(--neon-cyan),
  0 0 28px var(--neon-cyan);
```

**2. Add neon glow section** (after the `.try-again-btn:hover` rule, before `</style>`). Add these rules with the section comment:

```css
/* ---- Neon Glow (VIS-02) ---- */
.page-title {
  text-shadow: var(--neon-glow-green);
}

.countdown-value {
  text-shadow: var(--neon-glow-green);
}

.results-card-heading {
  text-shadow: var(--neon-glow-cyan);
}
```

Note: These are additive declarations. The existing `.page-title`, `.countdown-value`, and `.results-card-heading` rules higher in the stylesheet define layout/font properties. These new rules add only `text-shadow`. CSS cascade applies both sets.

**3. Add scanline overlay section** immediately after the neon glow section:

```css
/* ---- Scanlines Overlay (VIS-03) ---- */
body::after {
  content: '';
  position: fixed;
  inset: 0;
  background: repeating-linear-gradient(
    to bottom,
    transparent 0px,
    transparent 2px,
    rgba(0, 0, 0, 0.18) 2px,
    rgba(0, 0, 0, 0.18) 4px
  );
  pointer-events: none;
  z-index: 9999;
  -webkit-transform: translateZ(0);
  transform: translateZ(0);
}
```

Key constraints:
- `pointer-events: none` is mandatory — without it, the overlay blocks all card and button clicks
- `position: fixed` (not `absolute`) so the overlay covers the full viewport during scroll
- `rgba(0, 0, 0, 0.18)` opacity must not exceed 0.30 or text becomes unreadable
- `-webkit-transform: translateZ(0)` forces compositing layer to prevent iOS Safari flicker
- `z-index: 9999` places overlay above all content
  </action>
  <verify>
    <automated>grep -c 'neon-glow-green' index.html | xargs test 2 -le && grep -c 'neon-glow-cyan' index.html | xargs test 1 -le && grep -c 'repeating-linear-gradient' index.html | xargs test 1 -le && grep -c 'pointer-events: none' index.html | xargs test 1 -le && grep -c 'Neon Glow (VIS-02)' index.html | xargs test 1 -le && grep -c 'Scanlines Overlay (VIS-03)' index.html | xargs test 1 -le && echo "PASS: VIS-02 and VIS-03 CSS present" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html `:root` block contains `--neon-glow-green:` with exactly 5 text-shadow layers (5 commas separating them)
    - index.html `:root` block contains `--neon-glow-pink:` with exactly 4 text-shadow layers
    - index.html `:root` block contains `--neon-glow-cyan:` with exactly 4 text-shadow layers
    - index.html contains comment `/* ---- Neon Glow (VIS-02) ---- */`
    - index.html contains `.page-title { text-shadow: var(--neon-glow-green); }`
    - index.html contains `.countdown-value { text-shadow: var(--neon-glow-green); }`
    - index.html contains `.results-card-heading { text-shadow: var(--neon-glow-cyan); }`
    - index.html contains comment `/* ---- Scanlines Overlay (VIS-03) ---- */`
    - index.html contains `body::after {` with `position: fixed`
    - index.html `body::after` contains `pointer-events: none`
    - index.html `body::after` contains `z-index: 9999`
    - index.html `body::after` contains `repeating-linear-gradient` with `rgba(0, 0, 0, 0.18)` dark bands
    - index.html `body::after` contains `-webkit-transform: translateZ(0)`
    - No `text-shadow` appears inside any `@keyframes` block
    - `node scripts/verify-phase-2-results.js` still passes (engine not broken)
  </acceptance_criteria>
  <done>Neon glow text-shadow applied statically to .page-title, .countdown-value, and .results-card-heading via CSS custom properties. Scanline overlay on body::after with pointer-events: none and fixed positioning. Engine verification still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add glitch reveal animation and reduced-motion guard</name>
  <files>index.html</files>
  <read_first>
    - index.html (must re-read after Task 1 modifications — understand updated CSS structure and current showResults/resetResultState functions)
    - .planning/phases/03-visual-polish/03-RESEARCH.md (Pattern 3: Glitch/CRT Reveal Animation, Pattern 5: Reduced Motion, showResults() trigger code, resetResultState() cleanup code)
    - .planning/phases/03-visual-polish/03-UI-SPEC.md (Interaction Contract VIS-04 and Reduced Motion sections)
  </read_first>
  <action>
**1. Add glitch animation CSS section** after the scanlines overlay section, before `</style>`:

```css
/* ---- Glitch Animation (VIS-04) ---- */
.page-title {
  position: relative;
}

.page-title::before,
.page-title::after {
  content: attr(data-text);
  position: absolute;
  inset: 0;
  background: var(--bg-primary);
  overflow: hidden;
}

.page-title::before {
  color: var(--neon-cyan);
  clip-path: inset(0 0 60% 0);
}

.page-title::after {
  color: var(--neon-pink);
  clip-path: inset(55% 0 0 0);
}

.page-title.glitch-active::before {
  animation: glitch-top 0.6s steps(4) 1 forwards;
}

.page-title.glitch-active::after {
  animation: glitch-bot 0.6s steps(4) 1 forwards;
}

@keyframes glitch-top {
  0%   { clip-path: inset(0% 0 70% 0);  transform: translateX(-4px); }
  25%  { clip-path: inset(15% 0 50% 0); transform: translateX(3px);  }
  50%  { clip-path: inset(5% 0 65% 0);  transform: translateX(-2px); }
  75%  { clip-path: inset(20% 0 40% 0); transform: translateX(4px);  }
  100% { clip-path: inset(0 0 100% 0);  transform: translateX(0);    }
}

@keyframes glitch-bot {
  0%   { clip-path: inset(60% 0 0% 0);  transform: translateX(4px);  }
  25%  { clip-path: inset(70% 0 0 0);   transform: translateX(-3px); }
  50%  { clip-path: inset(55% 0 0 0);   transform: translateX(2px);  }
  75%  { clip-path: inset(75% 0 0 0);   transform: translateX(-4px); }
  100% { clip-path: inset(100% 0 0 0);  transform: translateX(0);    }
}
```

**2. Add reduced-motion CSS section** immediately after the glitch animation section:

```css
/* ---- Reduced Motion ---- */
@media (prefers-reduced-motion: reduce) {
  .page-title.glitch-active::before,
  .page-title.glitch-active::after {
    animation: none;
  }

  .results-panel.results-visible .results-reveal {
    transition-duration: 0.001ms;
  }
}
```

**3. Modify the `showResults(result)` function** in the `<script>` section. Inside the existing `requestAnimationFrame` callback (currently at approximately line 692-695), add the glitch trigger. The modified function body should look like this — insert 3 new lines BEFORE the existing `panel.classList.add('results-visible')` line:

Find the `requestAnimationFrame(function() {` block inside `showResults` and change it to:

```javascript
requestAnimationFrame(function() {
  var title = document.querySelector('#results-view .page-title');
  title.setAttribute('data-text', title.textContent);
  title.classList.add('glitch-active');
  panel.classList.add('results-visible');
  needle.style.transform = 'rotate(' + result.risk.needleAngle + 'deg)';
});
```

The three new lines are:
- `var title = document.querySelector('#results-view .page-title');` — selects only the results view title, not the input view title
- `title.setAttribute('data-text', title.textContent);` — sets the data-text attribute so `content: attr(data-text)` produces "Your fate is sealed" in the pseudo-elements
- `title.classList.add('glitch-active');` — triggers the CSS animation

**4. Modify the `resetResultState()` function.** Add one line at the end of the function body, before the closing `}`:

```javascript
document.querySelector('#results-view .page-title').classList.remove('glitch-active');
```

This cleans up the glitch class so the animation can replay on the next results reveal.

Key constraints:
- The glitch animation runs exactly ONCE per results reveal (`animation-iteration-count: 1`)
- Only `clip-path` and `transform: translateX` are animated — NO `text-shadow` or `box-shadow` in keyframes
- `data-text` MUST be set before `glitch-active` is added, otherwise pseudo-elements show empty colored blocks
- `glitch-active` is only added to `#results-view .page-title`, never to the input view title
- `forwards` fill mode keeps pseudo-elements collapsed (invisible) after animation completes
- Under `prefers-reduced-motion: reduce`, the glitch animation is completely suppressed via `animation: none`
  </action>
  <verify>
    <automated>grep -c 'glitch-active' index.html | xargs test 4 -le && grep -c 'glitch-top' index.html | xargs test 2 -le && grep -c 'glitch-bot' index.html | xargs test 2 -le && grep -c 'data-text' index.html | xargs test 1 -le && grep -c 'prefers-reduced-motion' index.html | xargs test 1 -le && grep -c 'Glitch Animation (VIS-04)' index.html | xargs test 1 -le && grep -c 'Reduced Motion' index.html | xargs test 1 -le && node scripts/verify-phase-2-results.js && echo "PASS: VIS-04 glitch and reduced motion present, engine OK" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains comment `/* ---- Glitch Animation (VIS-04) ---- */`
    - index.html contains `.page-title { position: relative; }` (in the glitch section)
    - index.html contains `.page-title::before,` and `.page-title::after {` with `content: attr(data-text)`
    - index.html contains `.page-title::before` with `color: var(--neon-cyan)` and `clip-path: inset(0 0 60% 0)`
    - index.html contains `.page-title::after` with `color: var(--neon-pink)` and `clip-path: inset(55% 0 0 0)`
    - index.html contains `.page-title.glitch-active::before` with `animation: glitch-top 0.6s steps(4) 1 forwards`
    - index.html contains `.page-title.glitch-active::after` with `animation: glitch-bot 0.6s steps(4) 1 forwards`
    - index.html contains `@keyframes glitch-top` with 5 keyframe stops (0%, 25%, 50%, 75%, 100%)
    - index.html contains `@keyframes glitch-bot` with 5 keyframe stops (0%, 25%, 50%, 75%, 100%)
    - Keyframes use ONLY `clip-path` and `transform` — grep for `text-shadow` inside `@keyframes` returns 0 matches
    - index.html contains comment `/* ---- Reduced Motion ---- */`
    - index.html contains `@media (prefers-reduced-motion: reduce)` with `animation: none` for glitch pseudo-elements
    - index.html contains `transition-duration: 0.001ms` inside the reduced-motion media query
    - `showResults` function contains `title.setAttribute('data-text', title.textContent)` BEFORE `title.classList.add('glitch-active')`
    - `showResults` function contains `document.querySelector('#results-view .page-title')`
    - `resetResultState` function contains `classList.remove('glitch-active')`
    - `node scripts/verify-phase-2-results.js` exits 0 (engine not broken)
  </acceptance_criteria>
  <done>Glitch reveal animation plays once on results title using clip-path + translateX keyframes. Pseudo-elements use attr(data-text) for ghost text layers. Reduced-motion media query suppresses all keyframe animations and reduces reveal transition to near-instant. Engine verification still passes.</done>
</task>

</tasks>

<verification>
1. `grep -c 'neon-glow' index.html` returns >= 6 (3 variable declarations + 3 usage sites)
2. `grep -c 'repeating-linear-gradient' index.html` returns >= 1
3. `grep -c 'pointer-events: none' index.html` returns >= 1
4. `grep -c 'glitch-active' index.html` returns >= 4 (CSS rules + JS add/remove)
5. `grep -c 'prefers-reduced-motion' index.html` returns >= 1
6. `node scripts/verify-phase-2-results.js` exits 0
7. No `text-shadow` inside any `@keyframes` block
</verification>

<success_criteria>
- VIS-02: .page-title, .countdown-value have green neon text-shadow; .results-card-heading has cyan neon text-shadow
- VIS-03: body::after scanline overlay with repeating-linear-gradient, fixed positioning, pointer-events: none, z-index: 9999
- VIS-04: Glitch animation on #results-view .page-title triggers once on showResults(), uses only clip-path/translateX in keyframes, data-text attribute set before glitch-active class
- Accessibility: prefers-reduced-motion suppresses all keyframe animations and reduces reveal transition duration
- Engine: verify-phase-2-results.js passes unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-polish/03-01-SUMMARY.md`
</output>
