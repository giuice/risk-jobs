---
phase: 02-engine-and-results
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified: [scripts/verify-phase-2-results.js, .planning/phases/02-engine-and-results/02-result-matrix.json]
autonomous: true
requirements: [CALC-01, CALC-02, CALC-03, CALC-04, RES-01, RES-02, RES-03]

must_haves:
  truths:
    - "The full 21x4 input matrix can be verified without a browser or third-party test framework"
    - "Every valid profession/seniority pair produces one deterministic result row in the verification output"
    - "Numeric collisions are reported as expected behavior under the locked score table, not as failures"
    - "A reusable matrix artifact exists for manual inspection and later debugging"
  artifacts:
    - path: "scripts/verify-phase-2-results.js"
      provides: "Node-only Phase 2 verification harness"
      contains: "Phase 2 matrix verified:"
      min_lines: 80
    - path: ".planning/phases/02-engine-and-results/02-result-matrix.json"
      provides: "Deterministic 84-row result artifact"
      contains: "\"matrixSize\": 84"
      min_lines: 20
  key_links:
    - from: "scripts/verify-phase-2-results.js"
      to: "index.html"
      via: "DATA and ENGINE section extraction"
      pattern: "===== DATA =====|===== ENGINE ====="
    - from: "buildResultMatrix"
      to: "02-result-matrix.json"
      via: "script writes normalized rows and metadata"
      pattern: "matrixSize|numericCollisionCount|rows"
---

<objective>
Create the Phase 2 verification harness that proves the locked 21x4 matrix is fully covered and generates a deterministic JSON artifact for inspection.

Purpose: Phase 2 requires proof of correctness across the full input matrix, and the roadmap language about “distinct adjusted score” must be translated into valid deterministic output coverage under the locked table.
Output: A Node-only verification script plus `.planning/phases/02-engine-and-results/02-result-matrix.json`.
</objective>

<execution_context>
@/home/giuice/.codex/get-shit-done/workflows/execute-plan.md
@/home/giuice/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-engine-and-results/02-CONTEXT.md
@.planning/phases/02-engine-and-results/02-RESEARCH.md
@index.html

<interfaces>
The verification harness must extract and execute the pure engine contract from `index.html`:

```js
{
  OCCUPATIONS,
  SENIORITY_LEVELS,
  buildResultModel,
  buildResultMatrix
}
```

The output artifact must be JSON with this top-level shape:

```json
{
  "occupationCount": 21,
  "seniorityCount": 4,
  "matrixSize": 84,
  "numericCollisionCount": 0,
  "rows": []
}
```
`numericCollisionCount` will likely be greater than `0` under the locked score table; that is expected and must not fail the script.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Node-only matrix verifier that extracts DATA and ENGINE from index.html</name>
  <files>scripts/verify-phase-2-results.js</files>
  <read_first>
    - index.html
    - .planning/phases/02-engine-and-results/02-RESEARCH.md
    - .planning/phases/02-engine-and-results/02-CONTEXT.md
  </read_first>
  <action>
Create `scripts/verify-phase-2-results.js` using only Node built-ins: `fs`, `path`, `vm`, and `assert/strict`.

Implement the script with these exact steps:

1. Resolve:
   - `const rootDir = path.resolve(__dirname, '..');`
   - `const htmlPath = path.join(rootDir, 'index.html');`
   - `const outputPath = path.join(rootDir, '.planning', 'phases', '02-engine-and-results', '02-result-matrix.json');`
2. Read `index.html` as UTF-8.
3. Extract the `DATA` block with regex `/\\/\\/ ===== DATA =====([\\s\\S]*?)\\/\\/ ===== ENGINE =====/` and the `ENGINE` block with regex `/\\/\\/ ===== ENGINE =====([\\s\\S]*?)\\/\\/ ===== I18N =====/`.
4. Build a VM script string that appends:
```js
module.exports = {
  OCCUPATIONS,
  SENIORITY_LEVELS,
  buildResultModel,
  buildResultMatrix
};
```
5. Evaluate in a sandbox context containing `module`, `exports`, `Math`, and `console`.
6. Assert these exact conditions:
   - `OCCUPATIONS.length === 21`
   - `SENIORITY_LEVELS.length === 4`
   - `buildResultMatrix().length === 84`
   - every row is non-null
   - every `adjustedScore` is `>= 0` and `<= 10`
   - every `shelfLifeYears` is `>= 0` and `<= 12`
   - every `countdown.years`, `countdown.months`, and `countdown.days` is an integer
   - every `risk.label` is a non-empty string
   - every `risk.needlePercent` and `risk.needleAngle` is a number
   - every `roast` is a non-empty string
7. Create a `Set` of `${row.occupation.id}::${row.seniority.id}` and assert the set size is `84`.
8. Count numeric collisions by grouping rows on `${row.adjustedScore}|${row.shelfLifeYears}`; do not treat collisions as failures.
9. Print one success line exactly starting with `Phase 2 matrix verified:` and include row count plus numeric collision count in the same line.

Do not require a browser, DOM shim, package install, or build step.
  </action>
  <verify>
    <automated>node scripts/verify-phase-2-results.js</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/verify-phase-2-results.js` exits 0
    - `grep -c 'const htmlPath = path.join(rootDir, '\''index.html'\'')' scripts/verify-phase-2-results.js` returns at least 1
    - `grep -c '===== DATA =====' scripts/verify-phase-2-results.js` returns at least 1
    - `grep -c '===== ENGINE =====' scripts/verify-phase-2-results.js` returns at least 1
    - `grep -c 'OCCUPATIONS.length === 21' scripts/verify-phase-2-results.js` returns at least 1
    - `grep -c 'SENIORITY_LEVELS.length === 4' scripts/verify-phase-2-results.js` returns at least 1
    - `grep -c 'buildResultMatrix().length === 84' scripts/verify-phase-2-results.js` returns at least 1
    - `grep -c 'Phase 2 matrix verified:' scripts/verify-phase-2-results.js` returns at least 1
    - `node scripts/verify-phase-2-results.js` exits 0
  </acceptance_criteria>
  <done>The repo contains a zero-dependency script that validates the full 84-row Phase 2 matrix directly from the shipped `index.html` engine code</done>
</task>

<task type="auto">
  <name>Task 2: Persist the deterministic matrix artifact for manual inspection and future debugging</name>
  <files>scripts/verify-phase-2-results.js, .planning/phases/02-engine-and-results/02-result-matrix.json</files>
  <read_first>
    - scripts/verify-phase-2-results.js
    - index.html
  </read_first>
  <action>
Extend `scripts/verify-phase-2-results.js` so a successful run always writes `.planning/phases/02-engine-and-results/02-result-matrix.json`.

The JSON must be pretty-printed with `JSON.stringify(output, null, 2)` and have this exact top-level structure:

```json
{
  "occupationCount": 21,
  "seniorityCount": 4,
  "matrixSize": 84,
  "numericCollisionCount": <number>,
  "rows": [
    {
      "occupationId": "...",
      "occupationName": "...",
      "seniorityId": "...",
      "seniorityLabel": "...",
      "adjustedScore": 0,
      "shelfLifeYears": 0,
      "countdown": { "years": 0, "months": 0, "days": 0, "totalDays": 0 },
      "risk": { "key": "...", "label": "...", "needlePercent": 0, "needleAngle": 0, "colorVar": "..." },
      "roast": "..."
    }
  ]
}
```

Build `rows` from `buildResultMatrix()` and normalize field names exactly as shown above. Keep row order deterministic by preserving the source order: every occupation in `OCCUPATIONS` outer loop and every seniority in `SENIORITY_LEVELS` inner loop.

After updating the script, run `node scripts/verify-phase-2-results.js` once so the JSON artifact exists on disk with the current engine output.
  </action>
  <verify>
    <automated>node scripts/verify-phase-2-results.js && grep -q '"matrixSize": 84' .planning/phases/02-engine-and-results/02-result-matrix.json && grep -q '"occupationCount": 21' .planning/phases/02-engine-and-results/02-result-matrix.json && grep -q '"seniorityCount": 4' .planning/phases/02-engine-and-results/02-result-matrix.json</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/02-engine-and-results/02-result-matrix.json` exits 0
    - `grep -c '"matrixSize": 84' .planning/phases/02-engine-and-results/02-result-matrix.json` returns at least 1
    - `grep -c '"occupationCount": 21' .planning/phases/02-engine-and-results/02-result-matrix.json` returns at least 1
    - `grep -c '"seniorityCount": 4' .planning/phases/02-engine-and-results/02-result-matrix.json` returns at least 1
    - `grep -c '"numericCollisionCount":' .planning/phases/02-engine-and-results/02-result-matrix.json` returns at least 1
    - `grep -c '"occupationId": "software-developers"' .planning/phases/02-engine-and-results/02-result-matrix.json` returns at least 1
    - `grep -c '"seniorityId": "architect"' .planning/phases/02-engine-and-results/02-result-matrix.json` returns at least 1
  </acceptance_criteria>
  <done>The verification script emits a deterministic JSON matrix artifact with 84 rows and explicit collision metadata that future phases can inspect</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/verify-phase-2-results.js` exits 0 and prints `Phase 2 matrix verified:`
- [ ] `.planning/phases/02-engine-and-results/02-result-matrix.json` exists with `matrixSize: 84`
- [ ] The script uses only Node built-ins and reads `index.html` directly
</verification>

<success_criteria>
- Phase 2 has an automated 21x4 proof-of-correctness harness
- Numeric collisions are surfaced as expected metadata instead of false failures
- The generated JSON artifact can be reused for debugging, manual inspection, and later content/share work
</success_criteria>

<output>
After completion, create `.planning/phases/02-engine-and-results/02-03-SUMMARY.md`
</output>
