---
phase: 02-engine-and-results
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [index.html]
autonomous: true
requirements: [RES-01, RES-02, RES-03, RES-04, RES-05]

must_haves:
  truths:
    - "After clicking the CTA, the results view shows a real countdown, gauge, and roast instead of placeholder text"
    - "The countdown appears first, the gauge reinforces severity second, and the roast lands third"
    - "Results reveal with a lightweight motion treatment and the gauge needle animates to the computed position"
    - "Clicking Try Again returns to the input view and clears the previous rendered result state"
  artifacts:
    - path: "index.html"
      provides: "Results layout, reveal styles, and UI wiring for render/reset flows"
      contains: "id=\"countdown-years\""
      min_lines: 520
  key_links:
    - from: "CTA click handler"
      to: "buildResultModel"
      via: "result object computed from occupation + years-range selection before showResults(result)"
      pattern: "buildResultModel\\(selectedOccupation, selectedExperience\\)"
    - from: "showResults(result)"
      to: "results DOM targets"
      via: "textContent assignments and needle transform"
      pattern: "countdown-years|risk-label|gauge-needle|roast-message"
    - from: "Try Again click handler"
      to: "fresh rerun state"
      via: "resetResultState() and showInput()"
      pattern: "resetResultState\\(\\)|showInput\\(\\)"
---

<objective>
Replace the Phase 1 placeholder results screen with a functional Phase 2 results experience: countdown hero, SVG risk gauge, placeholder roast message, lightweight reveal, and clean retry flow.

Purpose: The engine becomes useful only when the UI renders its result model in the locked countdown -> gauge -> roast order and resets cleanly between runs.
Output: `index.html` renders real result data on reveal and supports a fresh rerun without stale state.
</objective>

<execution_context>
@/home/giuice/.codex/get-shit-done/workflows/execute-plan.md
@/home/giuice/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-engine-and-results/02-CONTEXT.md
@.planning/phases/02-engine-and-results/02-RESEARCH.md
@index.html

<interfaces>
The UI in this plan must consume the Phase 2 engine contract directly:

```js
{
  occupation: { id, name, baseScore },
  seniority: { id, label, range, slevel },
  adjustedScore: Number,
  shelfLifeYears: Number,
  countdown: { years, months, days, totalDays },
  risk: { key, label, needlePercent, needleAngle, colorVar },
  roast: String
}
```

The results DOM targets required by research are:
`#result-job-name`, `#countdown-years`, `#countdown-months`, `#countdown-days`, `#risk-label`, `#risk-score`, `#gauge-needle`, `#roast-message`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the placeholder results screen with countdown, gauge, roast, and reveal styles</name>
  <files>index.html</files>
  <read_first>
    - index.html
    - .planning/phases/02-engine-and-results/02-CONTEXT.md
    - .planning/phases/02-engine-and-results/02-RESEARCH.md
  </read_first>
  <action>
Edit `index.html` and replace the current placeholder results markup plus minimal results styling with a real Phase 2 results layout. Keep this phase intentionally lightweight: no scanlines, no glitch text, no CRT spectacle.

Replace the `#results-content` placeholder block with a wrapper like this exact hierarchy:

1. `<div class="results-panel" id="results-panel">`
2. `<p class="result-job-name" id="result-job-name">--</p>`
3. Countdown card first:
   - `<section class="results-card results-reveal countdown-card">`
   - heading text `ESTIMATED SHELF LIFE`
   - three value boxes using ids `countdown-years`, `countdown-months`, `countdown-days`
   - labels `YEARS`, `MONTHS`, `DAYS`
4. Gauge card second:
   - `<section class="results-card results-reveal gauge-card">`
   - heading text `AUTOMATION DANGER`
   - inline SVG with `viewBox="0 0 240 160"`
   - background arc path from `M20 140 A100 100 0 0 1 220 140`
   - a line needle with `id="gauge-needle"`, `x1="120"`, `y1="140"`, `x2="120"`, `y2="52"`
   - center cap circle at `cx="120"`, `cy="140"`, `r="8"`
   - static edge labels `SAFE` and `DOOMED`
   - summary row containing `<span id="risk-label">--</span>` and `<span id="risk-score">0.0 / 10</span>`
5. Roast card third:
   - `<section class="results-card results-reveal roast-card">`
   - heading text `FINAL VERDICT`
   - `<p id="roast-message">--</p>`

Add or update CSS in the existing `<style>` block with these exact selectors and behaviors:
- `.results-panel` uses `display: grid`, `gap: 18px`, `max-width: 860px`, `margin: 0 auto`
- `.results-card` uses `background: rgba(22, 33, 62, 0.72)`, `border: 1px solid var(--text-dim)`, `border-radius: var(--border-radius)`, `padding: 20px`
- `.results-reveal` starts at `opacity: 0` and `transform: translateY(16px)`
- `.results-panel.results-visible .results-reveal` sets `opacity: 1`, `transform: translateY(0)`, `transition: opacity 220ms ease, transform 220ms ease`
- Stagger the reveal with:
  - `.results-panel.results-visible .countdown-card { transition-delay: 0ms; }`
  - `.results-panel.results-visible .gauge-card { transition-delay: 80ms; }`
  - `.results-panel.results-visible .roast-card { transition-delay: 160ms; }`
- `.countdown-grid` uses `display: grid`, `grid-template-columns: repeat(3, minmax(0, 1fr))`, `gap: 12px`
- `.countdown-value` uses `font-size: 3rem`, `color: var(--neon-green)`
- `.countdown-label` uses `font-size: 0.75rem`, `color: var(--text-secondary)`, `letter-spacing: 1px`
- `#gauge-needle` uses `transform-box: fill-box`, `transform-origin: center bottom`, `transform: rotate(-90deg)`, `transition: transform 900ms cubic-bezier(0.22, 1, 0.36, 1)`
- `#risk-label` uses `color: var(--neon-cyan)` by default and is later overwritten inline by JS
- `#roast-message` uses `font-size: 1rem`, `line-height: 1.6`, `color: var(--text-primary)`
- Add a mobile rule `@media (max-width: 640px)` that sets `.countdown-grid { grid-template-columns: 1fr; }` and `.countdown-value { font-size: 2.4rem; }`

Keep the existing page title and Try Again button container. The DOM order must remain countdown first, gauge second, roast third.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const html=fs.readFileSync('index.html','utf8'); ['id=\"results-panel\"','id=\"result-job-name\"','id=\"countdown-years\"','id=\"countdown-months\"','id=\"countdown-days\"','id=\"risk-label\"','id=\"risk-score\"','id=\"gauge-needle\"','id=\"roast-message\"','.results-panel.results-visible .results-reveal','transition: transform 900ms cubic-bezier(0.22, 1, 0.36, 1)','@media (max-width: 640px)'].forEach((snippet)=>{ if(!html.includes(snippet)) throw new Error('missing '+snippet); }); console.log('results layout contract present');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="results-panel"' index.html` returns 1
    - `grep -c 'id="result-job-name"' index.html` returns 1
    - `grep -c 'id="countdown-years"' index.html` returns 1
    - `grep -c 'id="countdown-months"' index.html` returns 1
    - `grep -c 'id="countdown-days"' index.html` returns 1
    - `grep -c 'id="risk-label"' index.html` returns 1
    - `grep -c 'id="risk-score"' index.html` returns 1
    - `grep -c 'id="gauge-needle"' index.html` returns 1
    - `grep -c 'id="roast-message"' index.html` returns 1
    - `grep -c '.results-panel.results-visible .results-reveal' index.html` returns at least 1
    - `grep -c 'transition: transform 900ms cubic-bezier(0.22, 1, 0.36, 1)' index.html` returns at least 1
    - `grep -c '@media (max-width: 640px)' index.html` returns at least 1
  </acceptance_criteria>
  <done>The results screen contains concrete countdown, gauge, and roast DOM targets plus the lightweight reveal and needle-transition CSS required for Phase 2</done>
</task>

<task type="auto">
  <name>Task 2: Wire CTA and Try Again flows to render and reset real result state</name>
  <files>index.html</files>
  <read_first>
    - index.html
    - .planning/phases/02-engine-and-results/02-RESEARCH.md
  </read_first>
  <action>
Edit the `// ===== UI =====` and `// ===== BOOT =====` sections in `index.html` to render the Phase 2 result model without duplicating engine logic.

Make these exact JS changes:

1. Rename the selection state variable from `selectedSeniority` to `selectedExperience` everywhere in the UI/BOOT flow, then add `let lastResult = null;` near `selectedOccupation` and `selectedExperience`.
   - `selectExperience(experienceId)` should store the clicked years-range selection with the exact assignment `selectedExperience = experienceId;`
   - `updateCTA()` should enable the CTA only when `selectedOccupation` and `selectedExperience` are both truthy
2. Replace `function showResults()` with `function showResults(result)` that:
   - assigns `lastResult = result`
   - hides `#input-view` and shows `#results-view`
   - fills DOM targets with:
     - `#result-job-name` => `result.occupation.name + ' // ' + result.seniority.label`
     - `#countdown-years` => `String(result.countdown.years)`
     - `#countdown-months` => `String(result.countdown.months)`
     - `#countdown-days` => `String(result.countdown.days)`
     - `#risk-label` => `result.risk.label`
     - `#risk-score` => `result.adjustedScore.toFixed(1) + ' / 10'`
     - `#roast-message` => `result.roast`
   - sets `#risk-label.style.color = 'var(' + result.risk.colorVar + ')'`
   - removes `results-visible` from `#results-panel`, sets `#gauge-needle.style.transform = 'rotate(-90deg)'`, then in `requestAnimationFrame` adds `results-visible` and sets `#gauge-needle.style.transform = 'rotate(' + result.risk.needleAngle + 'deg)'`
3. Add `function resetResultState()` that:
   - sets `lastResult = null`
   - removes `results-visible` from `#results-panel`
   - resets `#result-job-name` to `'--'`
   - resets countdown fields to `'0'`
   - resets `#risk-label` to `'--'`
   - resets `#risk-score` to `'0.0 / 10'`
   - resets `#risk-label.style.color` to `var(--neon-cyan)`
   - resets `#roast-message` to `'--'`
   - resets `#gauge-needle.style.transform` to `'rotate(-90deg)'`
4. Keep `function showInput()` limited to view toggling only.
5. Update the CTA click handler in `init()` so it does:
```js
var result = buildResultModel(selectedOccupation, selectedExperience);
if (result) {
  showResults(result);
}
```
6. Update the Try Again click handler so it does:
```js
resetResultState();
showInput();
```

Do not rebuild the results pane with one giant `innerHTML` string. Render into the stable DOM targets created in Task 1. Do not move formula logic into the click handlers.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const html=fs.readFileSync('index.html','utf8'); ['let selectedExperience = null;','let lastResult = null;','function showResults(result)','lastResult = result;','result.occupation.name + \\' // \\' + result.seniority.label','result.adjustedScore.toFixed(1) + \\' / 10\\'','requestAnimationFrame(function() {','results-visible','result.risk.needleAngle','function resetResultState()','lastResult = null;','buildResultModel(selectedOccupation, selectedExperience)','resetResultState();','showInput();'].forEach((snippet)=>{ if(!html.includes(snippet)) throw new Error('missing '+snippet); }); console.log('results wiring present');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'let selectedExperience = null;' index.html` returns 1
    - `grep -c 'let lastResult = null;' index.html` returns 1
    - `grep -c 'function showResults(result)' index.html` returns 1
    - `grep -c 'buildResultModel(selectedOccupation, selectedExperience)' index.html` returns at least 1
    - `grep -c 'selectedExperience = experienceId' index.html` returns at least 1
    - `grep -c 'result.adjustedScore.toFixed(1) + '\'' / 10'\''' index.html` returns at least 1
    - `grep -c 'function resetResultState()' index.html` returns 1
    - `grep -c 'gauge-needle.style.transform = '\''rotate(-90deg)'\''' index.html` returns at least 1
    - `grep -c 'requestAnimationFrame(function() {' index.html` returns at least 1
    - `grep -c 'resetResultState();' index.html` returns at least 1
    - `grep -c 'showInput();' index.html` returns at least 2
  </acceptance_criteria>
  <done>The UI renders real result objects on CTA, performs the lightweight reveal and gauge animation, and resets to a clean state on Try Again</done>
</task>

</tasks>

<verification>
- [ ] `node -e "const fs=require('fs'); const html=fs.readFileSync('index.html','utf8'); ['id=\"countdown-years\"','id=\"gauge-needle\"','let selectedExperience = null;','function showResults(result)','function resetResultState()','buildResultModel(selectedOccupation, selectedExperience)','requestAnimationFrame(function() {'].forEach((snippet)=>{ if(!html.includes(snippet)) throw new Error('missing '+snippet); }); console.log('phase-2-results-ui-ok');"` exits 0
- [ ] `grep -c '.results-panel.results-visible .results-reveal' index.html` returns at least 1
- [ ] `grep -c 'transition: transform 900ms cubic-bezier(0.22, 1, 0.36, 1)' index.html` returns at least 1
</verification>

<success_criteria>
- The placeholder results screen is gone
- `index.html` renders countdown, gauge, and roast in the locked hierarchy using stable DOM targets
- CTA and Try Again flows operate through `buildResultModel()` and `resetResultState()` rather than duplicated inline logic
</success_criteria>

<output>
After completion, create `.planning/phases/02-engine-and-results/02-02-SUMMARY.md`
</output>
